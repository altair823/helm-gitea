apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gitea-gateway
  namespace: gitea
spec:
  gatewayClassName: istio
  addresses:
    - value: "192.168.81.20"
  listeners:
    - name: http
      protocol: HTTP
      port: 3000
      allowedRoutes:
        namespaces:
          from: Same
